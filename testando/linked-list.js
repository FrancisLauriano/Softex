class Node{
    constructor(element){
        this.value = element;
        this.next = undefined;
    }
}

class Lista{
    constructor(){
        this.head = undefined;
        this.count = 0;

    }
}

//outros metodos da classe lista:
//1-push() adiciona um elemento no fim da lista;
//2-insertAt(element, position) adiciona um elemento em qualquer posição da lista
//3-remove() remove um elemento da lista
//4-getElementeAt(position) retorna um elemento de uma posição especifica da lista
//5-indexOf(element) retorna a posição de um elemento da lista
//6-removeAt(position) remove um elemento de uma posição especifica da lista
//7-isEmpty() checa se a lista esta vazia 
//8-size() retorna o tamanho da lista


